apiVersion: v1
kind: Service

metadata:
  name: preview-app

spec:
  type: {{ .Values.service.type }}

  selector:
    app: preview-app

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}